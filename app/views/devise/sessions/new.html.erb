<%# <div class="banner-login" >
  <div class="container">
    <h1>Une gestion facilitée de vos factures</h1>
    <p>Un outil 2-en-1. 100% adapté aux besoins de votre entreprise.</p>
  </div>
  </div>
</div> %>

<div class="d-flex justify-content-center invoice-form">

  <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="form-inputs">
      <h2 class="h2-form">Log in</h2>
      <%= f.input :email,
                  required: false,
                  autofocus: true,
                  input_html: { autocomplete: "email" } %>
      <%= f.input :password,
                  required: false,
                  input_html: { autocomplete: "current-password" } %>
      <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
    </div>

    <div class="form-actions">
      <%= f.button :submit, "Log in", class:"btn btn-flat btn-green" %>
    </div>
  <br>
  <%= render "devise/shared/links" %>
  <% end %>


</div>
